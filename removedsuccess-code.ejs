  <form action="/addmsg" method="POST">
      <div class="form-group">
          <textarea class="form-control" name="msg" id="msg" cols="30" rows="3" maxlength="200"
              placeholder="Enter Some Message.. 200 characters..."></textarea>
      </div>
      <div class="form-group">
          <button class="btn btn-outline-success" type="submit">Add Message</button>
      </div>
      <div>
          <% user.messages.forEach(function(msg, index) { %>
          <h4><%= msg %></h4>
          <% }) %>
      </div>
  </form>
